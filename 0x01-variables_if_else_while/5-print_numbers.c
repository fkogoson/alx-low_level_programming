#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: ALways 0 (Success)
 */
int main(void)
{
	/* your code goes here */
	int i;

	for (i = 0; i < 10;i++)
	{
		printf("%d",i);
	}
	putchar('\n');

	return (0);
}
